# 字幕对齐详解

## 📋 什么是"字幕对齐"？

在 VideoLingo 的处理流程中，**"字幕对齐"** 实际上指的是**两个不同层面的对齐**：

1. **双语对齐（Bilingual Alignment）** - `_5_split_sub.py`

   - 对齐源语言和目标语言的字幕分割
   - 确保两种语言的字幕**同步切分**

2. **时间戳对齐（Timestamp Alignment）** - `_6_gen_sub.py`
   - 将字幕句子与音频时间戳精确匹配
   - 确保字幕在**正确的时间点**显示

---

## 🎯 为什么需要对齐？

### 问题背景

经过翻译后，我们有：

```
源语言: ["Hello, how are you?", "I'm fine, thank you."]
译文:   ["你好，你好吗？", "我很好，谢谢。"]
```

但存在**两个问题**：

#### 问题 1: 字幕太长

```
源语言: "Which makes no sense to the average guy who always pushes the character creation slider all the way to the right."
译文:   "这对于总是将角色创建滑块一直推到右边的普通人来说是没有意义的。"

问题:
- 源语言字幕 115 个字符 ❌ (超过限制 75)
- 译文字幕显示宽度过长 ❌
```

#### 问题 2: 没有时间信息

```
源语言: "Hello, how are you?"
译文:   "你好，你好吗？"

问题: 不知道这句话应该在视频的哪个时间点显示 ❌
```

---

## 🔧 解决方案：两步对齐

```
翻译结果（无时间戳，可能超长）
    ↓
【步骤 1: 双语对齐】_5_split_sub.py
    检测超长字幕
    切分源语言字幕（2 部分）
    对齐切分目标语言字幕（保持同步）
    ↓
对齐后的字幕（长度合适，但还没时间戳）
    ↓
【步骤 2: 时间戳对齐】_6_gen_sub.py
    将字幕句子与词级时间戳匹配
    生成 SRT 格式（带时间轴）
    ↓
最终字幕文件（.srt）
```

---

## 📝 步骤 1: 双语对齐（`_5_split_sub.py`）

### 核心目标

确保**源语言**和**目标语言**的字幕**同步切分**，使它们：

1. 长度合适（不超过显示限制）
2. 一一对应（同时显示/消失）
3. 语义完整（切分点合理）

### 处理流程

```python
def split_align_subs(src_lines: List[str], tr_lines: List[str]):
    """
    双语对齐函数

    src_lines: 源语言句子列表
    tr_lines: 目标语言句子列表
    """

    # 1️⃣ 检测哪些字幕需要切分
    to_split = []
    for i, (src, tr) in enumerate(zip(src_lines, tr_lines)):
        # 检查是否超长
        if len(src) > MAX_SUB_LENGTH or calc_len(tr) * TARGET_SUB_MULTIPLIER > MAX_SUB_LENGTH:
            to_split.append(i)

    # 2️⃣ 对每个超长字幕进行切分
    for i in to_split:
        # 2a. 切分源语言字幕（使用 LLM）
        split_src = split_sentence(src_lines[i], num_parts=2)
        # 例如: "Hello, how are you? I'm fine."
        #   → "Hello, how are you?\nI'm fine."

        # 2b. 对齐切分目标语言字幕（使用 LLM）
        src_parts, tr_parts, tr_remerged = align_subs(
            src_lines[i],    # 原始源语言
            tr_lines[i],     # 原始译文
            split_src        # 切分后的源语言（带 \n）
        )
        # 例如:
        # src_parts = ["Hello, how are you?", "I'm fine."]
        # tr_parts  = ["你好，你好吗？", "我很好。"]

        # 更新列表
        src_lines[i] = src_parts
        tr_lines[i] = tr_parts

    # 3️⃣ 展平嵌套列表
    # 将 [["Hello"], "World", ["How", "Are"]]
    # 变成 ["Hello", "World", "How", "Are"]
    src_lines = flatten(src_lines)
    tr_lines = flatten(tr_lines)

    return src_lines, tr_lines
```

### 示例演示

#### 输入（翻译后的结果）

```python
src_lines = [
    "Hello, how are you?",
    "Which makes no sense to the average guy who always pushes the slider.",  # 超长！
    "Thank you very much."
]

tr_lines = [
    "你好，你好吗？",
    "这对于总是将滑块推到右边的普通人来说是没有意义的。",  # 超长！
    "非常感谢。"
]
```

#### 步骤 1: 检测超长字幕

```python
MAX_SUB_LENGTH = 75

# 检查源语言
len("Which makes no sense...") = 76  ❌ 超长！

# 检查译文（考虑字符宽度）
calc_len("这对于总是...") * 1.2 = 25 * 1.75 * 1.2 = 52.5  ✅ (其实这个例子没超)

# 标记需要切分: to_split = [1]
```

#### 步骤 2: 切分源语言

使用 LLM 切分（调用 `split_sentence`）：

**Prompt 给 LLM**:

```
Split this sentence into 2 parts:
"Which makes no sense to the average guy who always pushes the slider."

Output with [br] tag.
```

**LLM 返回**:

```json
{
  "split1": "Which makes no sense to the average guy [br] who always pushes the slider.",
  "split2": "Which makes no sense [br] to the average guy who always pushes the slider.",
  "choice": "1"
}
```

**解析后**:

```python
split_src = "Which makes no sense to the average guy\nwho always pushes the slider."
```

#### 步骤 3: 对齐译文切分

使用 LLM 对齐（调用 `align_subs`）：

**Prompt 给 LLM**:

```
Source Original: "Which makes no sense to the average guy who always pushes the slider."
Target Original: "这对于总是将滑块推到右边的普通人来说是没有意义的。"
Source Split: "Which makes no sense to the average guy [br] who always pushes the slider."

Please split the target subtitle to align with the source split.
```

**LLM 返回**:

```json
{
  "align": [
    {
      "src_part_1": "Which makes no sense to the average guy",
      "target_part_1": "这对于普通人来说是没有意义的"
    },
    {
      "src_part_2": "who always pushes the slider.",
      "target_part_2": "他们总是将滑块推到右边。"
    }
  ]
}
```

**结果**:

```python
src_parts = [
    "Which makes no sense to the average guy",
    "who always pushes the slider."
]

tr_parts = [
    "这对于普通人来说是没有意义的",
    "他们总是将滑块推到右边。"
]
```

#### 步骤 4: 更新列表

```python
# 原来
src_lines[1] = "Which makes no sense to the average guy who always pushes the slider."
tr_lines[1] = "这对于总是将滑块推到右边的普通人来说是没有意义的。"

# 现在
src_lines[1] = [
    "Which makes no sense to the average guy",
    "who always pushes the slider."
]
tr_lines[1] = [
    "这对于普通人来说是没有意义的",
    "他们总是将滑块推到右边。"
]
```

#### 步骤 5: 展平列表

```python
# 展平前
src_lines = [
    "Hello, how are you?",
    ["Which makes no sense to the average guy", "who always pushes the slider."],
    "Thank you very much."
]

# 展平后
src_lines = [
    "Hello, how are you?",
    "Which makes no sense to the average guy",
    "who always pushes the slider.",
    "Thank you very much."
]
```

同样处理 `tr_lines`。

#### 最终输出

```python
src_lines = [
    "Hello, how are you?",
    "Which makes no sense to the average guy",
    "who always pushes the slider.",
    "Thank you very much."
]

tr_lines = [
    "你好，你好吗？",
    "这对于普通人来说是没有意义的",
    "他们总是将滑块推到右边。",
    "非常感谢。"
]
```

✅ **对齐完成**：源语言和译文的句子数量相同（4 句对 4 句）

---

## 🕐 步骤 2: 时间戳对齐（`_6_gen_sub.py`）

### 核心目标

将**已切分好的字幕句子**与**词级时间戳**精确匹配，生成带时间轴的 SRT 文件。

### 输入数据

#### 1. 词级时间戳数据（来自 WhisperX）

```python
df_words = pd.DataFrame({
    'text':  ['Hello', ',', 'how', 'are', 'you', '?', 'Which', 'makes', ...],
    'start': [0.0,     0.5,  0.6,  0.8,  1.0,  1.2,  1.5,     1.8,      ...],
    'end':   [0.5,     0.6,  0.8,  1.0,  1.2,  1.4,  1.8,     2.1,      ...]
})
```

#### 2. 切分后的字幕句子

```python
df_sentences = pd.DataFrame({
    'Source': [
        "Hello, how are you?",
        "Which makes no sense to the average guy",
        "who always pushes the slider.",
        "Thank you very much."
    ],
    'Translation': [
        "你好，你好吗？",
        "这对于普通人来说是没有意义的",
        "他们总是将滑块推到右边。",
        "非常感谢。"
    ]
})
```

### 处理流程

#### 步骤 1: 构建完整字符串

```python
# 将所有词连接起来（去除标点和空格）
full_words_str = ''
position_to_word_idx = {}

for idx, word in enumerate(df_words['text']):
    clean_word = remove_punctuation(word.lower())  # "Hello," → "hello"
    start_pos = len(full_words_str)
    full_words_str += clean_word

    # 记录每个字符位置对应的词索引
    for pos in range(start_pos, len(full_words_str)):
        position_to_word_idx[pos] = idx

# 结果:
# full_words_str = "hellohowareyouwhichmakesnosense..."
# position_to_word_idx = {
#     0: 0,  # 'h' 属于第 0 个词 "Hello"
#     1: 0,  # 'e' 属于第 0 个词 "Hello"
#     2: 0,  # 'l' 属于第 0 个词 "Hello"
#     ...
#     5: 2,  # 'h' 属于第 2 个词 "how"
#     ...
# }
```

#### 步骤 2: 匹配字幕句子

```python
current_pos = 0
time_stamp_list = []

for sentence in df_sentences['Source']:
    # 清理句子（去除标点和空格）
    clean_sentence = remove_punctuation(sentence.lower()).replace(" ", "")
    # "Hello, how are you?" → "hellohowareyou"

    sentence_len = len(clean_sentence)  # 14

    # 在 full_words_str 中查找匹配
    match_found = False
    while current_pos <= len(full_words_str) - sentence_len:
        if full_words_str[current_pos:current_pos+sentence_len] == clean_sentence:
            # 找到匹配！

            # 获取开始和结束的词索引
            start_word_idx = position_to_word_idx[current_pos]  # 例如: 0
            end_word_idx = position_to_word_idx[current_pos + sentence_len - 1]  # 例如: 5

            # 获取时间戳
            start_time = df_words['start'][start_word_idx]  # 0.0
            end_time = df_words['end'][end_word_idx]        # 1.4

            time_stamp_list.append((start_time, end_time))

            current_pos += sentence_len
            match_found = True
            break

        current_pos += 1

    if not match_found:
        raise ValueError(f"❎ No match found for sentence: {sentence}")
```

#### 步骤 3: 生成时间戳

```python
# 为每个字幕分配时间戳
df_sentences['timestamp'] = time_stamp_list

# 结果:
# df_sentences = pd.DataFrame({
#     'Source': ["Hello, how are you?", ...],
#     'Translation': ["你好，你好吗？", ...],
#     'timestamp': [(0.0, 1.4), (1.5, 3.2), ...]
# })
```

#### 步骤 4: 去除间隙

```python
# 消除字幕之间的小间隙（< 1 秒）
for i in range(len(df_sentences)-1):
    delta_time = df_sentences['timestamp'][i+1][0] - df_sentences['timestamp'][i][1]
    # 例如: 下一个字幕 1.8s 开始，当前字幕 1.4s 结束 → 间隙 0.4s

    if 0 < delta_time < 1:
        # 延长当前字幕的结束时间到下一个字幕的开始时间
        df_sentences.at[i, 'timestamp'] = (
            df_sentences['timestamp'][i][0],  # 保持开始时间
            df_sentences['timestamp'][i+1][0]  # 结束时间 → 下一个的开始时间
        )

# 例如: (0.0, 1.4) → (0.0, 1.8)  ✅ 无缝衔接
```

#### 步骤 5: 转换为 SRT 格式

```python
def convert_to_srt_format(start_time, end_time):
    """将秒数转换为 SRT 时间格式"""
    def seconds_to_hmsm(seconds):
        hours = int(seconds // 3600)
        minutes = int((seconds % 3600) // 60)
        seconds = seconds % 60
        milliseconds = int(seconds * 1000) % 1000
        return f"{hours:02d}:{minutes:02d}:{int(seconds):02d},{milliseconds:03d}"

    start_srt = seconds_to_hmsm(start_time)
    end_srt = seconds_to_hmsm(end_time)
    return f"{start_srt} --> {end_srt}"

# 例如:
# (0.0, 1.8) → "00:00:00,000 --> 00:00:01,800"
# (1.8, 3.2) → "00:00:01,800 --> 00:00:03,200"
```

#### 步骤 6: 生成 SRT 文件

```python
def generate_subtitle_string(df, columns):
    """生成 SRT 字符串"""
    srt_str = ''
    for i, row in df.iterrows():
        srt_str += f"{i+1}\n"  # 字幕编号
        srt_str += f"{row['timestamp']}\n"  # 时间轴
        srt_str += f"{row[columns[0]].strip()}\n"  # 第一行（源语言或译文）
        if len(columns) > 1:
            srt_str += f"{row[columns[1]].strip()}\n"  # 第二行（双语）
        srt_str += "\n"  # 空行分隔
    return srt_str

# 生成不同格式的 SRT
generate_subtitle_string(df, ['Source'])  # 仅源语言
generate_subtitle_string(df, ['Translation'])  # 仅译文
generate_subtitle_string(df, ['Source', 'Translation'])  # 双语
```

### 最终输出示例

#### src.srt（源语言字幕）

```srt
1
00:00:00,000 --> 00:00:01,800
Hello, how are you?

2
00:00:01,800 --> 00:00:03,200
Which makes no sense to the average guy

3
00:00:03,200 --> 00:00:04,500
who always pushes the slider.

4
00:00:04,500 --> 00:00:06,000
Thank you very much.
```

#### trans.srt（译文字幕）

```srt
1
00:00:00,000 --> 00:00:01,800
你好，你好吗？

2
00:00:01,800 --> 00:00:03,200
这对于普通人来说是没有意义的

3
00:00:03,200 --> 00:00:04,500
他们总是将滑块推到右边。

4
00:00:04,500 --> 00:00:06,000
非常感谢。
```

#### src_trans.srt（双语字幕）

```srt
1
00:00:00,000 --> 00:00:01,800
Hello, how are you?
你好，你好吗？

2
00:00:01,800 --> 00:00:03,200
Which makes no sense to the average guy
这对于普通人来说是没有意义的

3
00:00:03,200 --> 00:00:04,500
who always pushes the slider.
他们总是将滑块推到右边。

4
00:00:04,500 --> 00:00:06,000
Thank you very much.
非常感谢。
```

---

## 🎯 两步对齐的关系

```
┌─────────────────────────────────────────────────────────┐
│  步骤 1: 双语对齐 (_5_split_sub.py)                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━        │
│                                                         │
│  输入:                                                  │
│    源语言: ["Hello", "超长句子...", "Thank you"]        │
│    译文:   ["你好", "超长翻译...", "谢谢"]              │
│                                                         │
│  处理:                                                  │
│    1. 检测超长字幕                                      │
│    2. 切分源语言（LLM）                                 │
│    3. 对齐切分译文（LLM）                               │
│    4. 展平列表                                          │
│                                                         │
│  输出:                                                  │
│    源语言: ["Hello", "超长句1", "超长句2", "Thank you"] │
│    译文:   ["你好", "翻译1", "翻译2", "谢谢"]           │
│                                                         │
│  ✅ 确保: 源语言和译文句子数量相同，长度合适            │
└─────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 2: 时间戳对齐 (_6_gen_sub.py)                     │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━        │
│                                                         │
│  输入:                                                  │
│    词级时间戳: [("Hello", 0.0, 0.5), ...]              │
│    字幕句子: ["Hello", "超长句1", ...]                  │
│                                                         │
│  处理:                                                  │
│    1. 构建完整字符串                                    │
│    2. 匹配字幕句子                                      │
│    3. 获取时间戳                                        │
│    4. 去除间隙                                          │
│    5. 生成 SRT                                          │
│                                                         │
│  输出:                                                  │
│    src.srt, trans.srt, src_trans.srt, trans_src.srt    │
│                                                         │
│  ✅ 确保: 每个字幕都有准确的显示时间                    │
└─────────────────────────────────────────────────────────┘
```

---

## 💡 关键理解点

### 1. 为什么需要两步对齐？

**双语对齐**解决的问题：

- ❓ 源语言和译文的句子数量不匹配
- ❓ 字幕太长无法显示

**时间戳对齐**解决的问题：

- ❓ 字幕不知道何时显示/消失
- ❓ 字幕与音频不同步

### 2. "对齐"到底是什么意思？

#### 双语对齐 = 同步切分

```
源语言: "AB"       译文: "甲乙"
         ↓                ↓
        "A"  "B"         "甲"  "乙"
         └────┬────┘      └────┬────┘
              对齐（一一对应）
```

如果不对齐：

```
源语言: "A"  "B"
译文:   "甲乙"        ❌ 显示时间不同步！
```

#### 时间戳对齐 = 匹配音频时间

```
字幕: "Hello"
        ↓
音频: [0.0s - 0.5s] 说了 "Hello"
        ↓
SRT:  00:00:00,000 --> 00:00:00,500
      Hello
```

### 3. 为什么使用 LLM？

#### 双语对齐为什么用 LLM？

**问题**：不同语言的语序不同

```
英语: "I love you"    → 切分 → "I love" | "you"
中文: "我爱你"        → 如何对齐切分？

简单对应（错误）:
"I love" → "我爱"
"you"    → "你"

LLM 智能对应（正确）:
"I love" → "我"
"you"    → "爱你"
或
"I love" → "我爱"
"you"    → "你"  ← LLM 根据语义选择最佳切分
```

#### 时间戳对齐为什么不用 LLM？

**因为是精确匹配**，使用算法更准确：

```
字符串匹配算法:
"Hello, how are you?" → "hellohowareyou"
                         ↓ 精确查找
"hellohowareyou..." → 位置: 0-14
                         ↓ 映射到词
词 0-5: "Hello", ",", "how", "are", "you", "?"
                         ↓ 获取时间
时间: (0.0, 1.4)
```

---

## 🔍 实例演示

### 完整流程演示

#### 输入（翻译后）

```python
# 假设有一个超长句子
source = "Hello everyone, this is a very long sentence that exceeds the maximum subtitle length limit."
translation = "大家好，这是一个超过字幕最大长度限制的非常长的句子。"
```

#### 步骤 1: 双语对齐

```python
# 1. 检测超长
len(source) = 97  ❌ 超过 75

# 2. 切分源语言（LLM）
split_src = "Hello everyone, this is a very long sentence\nthat exceeds the maximum subtitle length limit."

# 3. 对齐译文（LLM）
src_parts = [
    "Hello everyone, this is a very long sentence",
    "that exceeds the maximum subtitle length limit."
]
tr_parts = [
    "大家好，这是一个非常长的句子",
    "超过了字幕最大长度限制。"
]
```

#### 步骤 2: 时间戳对齐

```python
# 词级时间戳（WhisperX 输出）
words_time = [
    ("Hello", 0.0, 0.3),
    ("everyone", 0.4, 0.8),
    (",", 0.8, 0.9),
    ("this", 1.0, 1.2),
    ("is", 1.2, 1.3),
    ("a", 1.3, 1.4),
    ("very", 1.4, 1.6),
    ("long", 1.7, 1.9),
    ("sentence", 2.0, 2.5),
    ("that", 2.6, 2.8),
    ("exceeds", 2.9, 3.3),
    ...
]

# 匹配第一句
sentence1 = "Hello everyone, this is a very long sentence"
clean = "helloeveryonethisisaverylongsentence"
        ↓ 在完整字符串中匹配
位置: 0 → 对应词 0-8
时间: (0.0, 2.5)  ✅

# 匹配第二句
sentence2 = "that exceeds the maximum subtitle length limit."
clean = "thatexceedsthemaximumsubtitlelengthlimit"
        ↓ 在完整字符串中匹配
位置: 38 → 对应词 9-15
时间: (2.6, 5.0)  ✅
```

#### 最终 SRT 输出

```srt
1
00:00:00,000 --> 00:00:02,500
Hello everyone, this is a very long sentence
大家好，这是一个非常长的句子

2
00:00:02,600 --> 00:00:05,000
that exceeds the maximum subtitle length limit.
超过了字幕最大长度限制。
```

---

## 📊 总结对比

| 方面         | 双语对齐 (步骤 1)      | 时间戳对齐 (步骤 2)    |
| ------------ | ---------------------- | ---------------------- |
| **目的**     | 同步切分源语言和译文   | 将字幕匹配到音频时间   |
| **输入**     | 翻译后的句子对         | 词级时间戳 + 字幕句子  |
| **输出**     | 对齐的句子对（无时间） | 带时间戳的 SRT 文件    |
| **核心技术** | LLM（语义理解）        | 字符串匹配算法         |
| **解决问题** | 字幕太长 + 语序差异    | 字幕显示时间           |
| **关键文件** | `_5_split_sub.py`      | `_6_gen_sub.py`        |
| **中间产物** | `_5_split_sub.xlsx`    | `src.srt`, `trans.srt` |

---

## 🎯 核心要点

1. **"对齐"有两层含义**：

   - 双语对齐 = 源语言和译文同步切分
   - 时间戳对齐 = 字幕匹配音频时间

2. **为什么要对齐**：

   - 确保字幕长度合适（Netflix 标准）
   - 确保双语字幕同步显示
   - 确保字幕在正确时间出现

3. **谁用 LLM，谁用算法**：

   - 双语对齐用 LLM（需要语义理解）
   - 时间戳对齐用算法（精确匹配）

4. **最终目标**：
   - 生成高质量的 SRT 字幕文件
   - 完美同步于视频音频

---

**文档生成时间**: 2025-12-29  
**VideoLingo 版本**: 3.0.0
