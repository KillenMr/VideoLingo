# VideoLingo 项目能力分析报告

## 📋 项目概述

**VideoLingo** 是一个全链路视频翻译、本地化和配音工具，目标是生成 Netflix 级别的字幕质量。

### 核心特性

- 🎥 YouTube 视频下载（通过 yt-dlp）
- 🎙️ WhisperX 词级精准字幕识别
- 📝 NLP + AI 驱动的字幕分割
- 📚 自定义术语表 + AI 生成术语
- 🔄 三步翻译流程（Translate-Reflect-Adaptation）
- ✅ Netflix 标准单行字幕
- 🗣️ 多种 TTS 配音支持
- 🚀 Streamlit Web 界面一键处理

---

## 🏗️ 整体架构

### 1. Web 端架构（Streamlit）

**入口文件**: `st.py`

Web 端提供三大核心功能模块：

#### a. 视频下载模块

- 支持 YouTube 链接下载
- 支持本地视频上传（mp4, mov, avi, mkv, flv, wmv, webm）
- 支持音频格式（wav, mp3, flac, m4a）

#### b. 字幕翻译生成模块（重点）

包含以下步骤：

1. WhisperX 词级转录
2. NLP 和 LLM 句子分割
3. 摘要和多步翻译
4. 长字幕切割和对齐
5. 时间轴生成和字幕输出
6. 字幕合并到视频

#### c. 配音模块

包含以下步骤：

1. 生成音频任务和块
2. 提取参考音频
3. 生成和合并音频文件
4. 合并音频到视频

### 2. 核心处理流程

```
视频输入
   ↓
_1_ytdlp.py          → 视频下载/定位
   ↓
_2_asr.py            → ASR 转录（WhisperX）
   ↓
_3_1_split_nlp.py    → NLP 分割（4步处理）
   ↓
_3_2_split_meaning.py → 基于语义的 LLM 分割
   ↓
_4_1_summarize.py    → 内容摘要和术语提取
   ↓
_4_2_translate.py    → 多线程翻译
   ↓
_5_split_sub.py      → 字幕长度切割和对齐
   ↓
_6_gen_sub.py        → 时间戳对齐和 SRT 生成
   ↓
_7_sub_into_vid.py   → 字幕烧录到视频
   ↓
_8-12 (配音流程)    → 音频生成和合并
```

---

## 🎯 字幕生成与分割逻辑深度分析

### 一、ASR 转录阶段 (`_2_asr.py`)

#### 核心功能

1. **视频转音频**: 使用 ffmpeg 提取音频
2. **人声分离（可选）**: 使用 Demucs 分离人声和背景音乐
3. **音量标准化**: 标准化人声音频音量
4. **分段转录**: 将音频切分为多个片段进行 WhisperX 转录
5. **结果合并**: 合并所有片段的转录结果

#### 支持的转录模式

- **local**: 本地运行 WhisperX (large-v3/large-v3-turbo)
- **cloud**: 使用 302.ai API
- **elevenlabs**: 使用 ElevenLabs API（实验性）

#### 输出

- `output/log/cleaned_chunks.xlsx`: 清理后的词级时间戳数据

---

### 二、NLP 分割阶段 (`_3_1_split_nlp.py`)

这是字幕处理的**第一阶段分割**，使用 NLP 技术进行基于规则的句子切分。

#### 四步分割流程

```python
def split_by_spacy():
    nlp = init_nlp()
    split_by_mark(nlp)           # 步骤1: 按标点符号分割
    split_by_comma_main(nlp)     # 步骤2: 按逗号分割
    split_sentences_main(nlp)    # 步骤3: 按连接词分割
    split_long_by_root_main(nlp) # 步骤4: 按句法根分割长句
```

#### 步骤 1: 按标点符号分割 (`split_by_mark.py`)

**核心逻辑**:

- 使用 spaCy 的句子边界检测（SENT_START）
- 特殊处理破折号（-）和省略号（...）连接的句子，保持其完整性
- 处理中文、日文等语言的标点符号合并问题

**输入**: `output/log/cleaned_chunks.xlsx`（词级数据）
**输出**: `output/log/split_by_mark.txt`

**关键代码**:

```python
for sent in doc.sents:
    text = sent.text.strip()

    # 如果句子以 - 或 ... 开始/结束，则与前句合并
    if current_sentence and (
        text.startswith('-') or
        text.startswith('...') or
        current_sentence[-1].endswith('-') or
        current_sentence[-1].endswith('...')
    ):
        current_sentence.append(text)
    else:
        if current_sentence:
            sentences_by_mark.append(' '.join(current_sentence))
            current_sentence = []
        current_sentence.append(text)
```

#### 步骤 2: 按逗号分割 (`split_by_comma.py`)

**核心逻辑**:

- 智能分析逗号两侧的短语结构
- 只有当两侧都是有效短语（有主语和动词）且足够长时才分割
- 使用依存句法分析判断短语完整性

**关键参数**:

- `comma_split_threshold`: 最小单词数阈值（默认 3）

**验证逻辑**:

```python
def is_valid_phrase(phrase):
    # 检查是否有主语和动词
    has_subject = any(token.dep_ in ["nsubj", "nsubjpass"] or token.pos_ == "PRON" for token in phrase)
    has_verb = any((token.pos_ == "VERB" or token.pos_ == 'AUX') for token in phrase)
    return (has_subject and has_verb)

def analyze_comma(start, doc, token):
    left_phrase = doc[max(start, token.i - 9):token.i]
    right_phrase = doc[token.i + 1:min(len(doc), token.i + 10)]

    suitable_for_splitting = is_valid_phrase(right_phrase)

    # 检查词数
    left_words = [t for t in left_phrase if not t.is_punct]
    right_words = list(itertools.takewhile(lambda t: not t.is_punct, right_phrase))

    threshold = load_key("nlp.comma_split_threshold")
    if len(left_words) <= threshold or len(right_words) <= threshold:
        suitable_for_splitting = False

    return suitable_for_splitting
```

**输入**: `output/log/split_by_mark.txt`
**输出**: `output/log/split_by_comma.txt`

#### 步骤 3: 按连接词分割 (`split_by_connector.py`)

**核心逻辑**:

- 识别各语言的关键连接词（that, which, where, because, but, and, or 等）
- 使用依存句法分析判断连接词的功能
- 避免错误分割（如缩写、定语从句）

**支持语言**:

- 英语: that, which, where, when, because, but, and, or
- 中文: 因为, 所以, 但是, 而且, 虽然, 如果, 即使, 尽管
- 日语: けれども, しかし, だから, それで, ので, のに, ため
- 法语、俄语、西班牙语、德语、意大利语等

**关键代码**:

```python
def analyze_connectors(doc, token):
    if lang == "en" and token.text.lower() == "that":
        if token.dep_ == mark_dep and token.head.pos_ == verb_pos:
            return True, False  # 分割
        else:
            return False, False  # 不分割（可能是定语从句）
    elif token.dep_ in det_pron_deps and token.head.pos_ in noun_pos:
        return False, False  # 作为限定词或代词修饰名词，不分割
    else:
        return True, False
```

**输入**: `output/log/split_by_comma.txt`
**输出**: `output/log/split_by_connector.txt`

#### 步骤 4: 按句法根分割长句 (`split_long_by_root.py`)

**核心逻辑**:

- 使用动态规划寻找最优分割点
- 优先在句子结束、动词、助动词或句法根处分割
- 处理超长句子（超过 `max_sentence_length`）

**两级分割策略**:

1. **智能分割** (`split_long_sentence`):

```python
def split_long_sentence(doc):
    # 动态规划，在 VERB, AUX, ROOT 处分割
    dp = [float('inf')] * (n + 1)
    dp[0] = 0

    for i in range(1, n + 1):
        for j in range(max(0, i - 100), i):
            if i - j >= min_split_length:
                token = doc[i-1]
                if j == 0 or (token.is_sent_end or token.pos_ in ['VERB', 'AUX'] or token.dep_ == 'ROOT'):
                    if dp[j] + 1 < dp[i]:
                        dp[i] = dp[j] + 1
                        prev[i] = j
```

2. **强制分割** (`split_extremely_long_sentence`):

- 当句子仍然超长时，按固定长度强制分割

**关键参数**:

- `nlp.max_sentence_length`: 最大句子长度（默认 60）
- `nlp.min_split_length`: 最小分割长度（默认 30）

**输入**: `output/log/split_by_connector.txt`
**输出**: `output/log/_3_1_nlp_processed.txt`

---

### 三、基于语义的 LLM 分割阶段 (`_3_2_split_meaning.py`)

经过 NLP 分割后，某些句子可能仍然过长（超过 `max_split_length`，默认 20 词）。此阶段使用 **LLM** 进行语义感知的智能分割。

#### 核心流程

1. **分词**: 使用 spaCy 分词统计词数
2. **计算分割数**: `num_parts = ceil(词数 / max_split_length)`
3. **LLM 分割**: 调用 GPT 进行语义分割
4. **位置映射**: 使用字符串相似度匹配找到分割点
5. **多轮迭代**: 最多 3 轮，确保所有句子都被充分分割

#### LLM Prompt (`get_split_prompt`)

```
## Role
You are a professional Netflix subtitle splitter in **{language}**.

## Task
Split the given subtitle text into **{num_parts}** parts, each less than **{word_limit}** words.

1. Maintain sentence meaning coherence according to Netflix subtitle standards
2. MOST IMPORTANT: Keep parts roughly equal in length (minimum 3 words each)
3. Split at natural points like punctuation marks or conjunctions
4. If provided text is repeated words, simply split at the middle of the repeated words.

## Steps
1. Analyze the sentence structure, complexity, and key splitting challenges
2. Generate two alternative splitting approaches with [br] tags at split positions
3. Compare both approaches highlighting their strengths and weaknesses
4. Choose the best splitting approach

## Given Text
<split_this_sentence>
{sentence}
</split_this_sentence>

## Output in only JSON format
{
    "analysis": "Brief description of sentence structure...",
    "split1": "First splitting approach with [br] tags...",
    "split2": "Alternative splitting approach with [br] tags...",
    "assess": "Comparison of both approaches...",
    "choice": "1 or 2"
}
```

#### 分割点查找算法

使用 **序列匹配器（SequenceMatcher）** 找到 LLM 返回的 `[br]` 标记位置：

```python
def find_split_positions(original, modified):
    split_positions = []
    parts = modified.split('[br]')
    start = 0

    for i in range(len(parts) - 1):
        max_similarity = 0
        best_split = None

        for j in range(start, len(original)):
            original_left = original[start:j]
            modified_left = joiner.join(parts[i].split())

            left_similarity = SequenceMatcher(None, original_left, modified_left).ratio()

            if left_similarity > max_similarity:
                max_similarity = left_similarity
                best_split = j

        if max_similarity < 0.9:
            console.print(f"[yellow]Warning: low similarity {max_similarity}[/yellow]")

        if best_split is not None:
            split_positions.append(best_split)
            start = best_split

    return split_positions
```

#### 并发处理

使用线程池并发处理多个句子：

```python
def parallel_split_sentences(sentences, max_length, max_workers, nlp, retry_attempt=0):
    new_sentences = [None] * len(sentences)
    futures = []

    with concurrent.futures.ThreadPoolExecutor(max_workers=max_workers) as executor:
        for index, sentence in enumerate(sentences):
            tokens = tokenize_sentence(sentence, nlp)
            num_parts = math.ceil(len(tokens) / max_length)
            if len(tokens) > max_length:
                future = executor.submit(split_sentence, sentence, num_parts, max_length, index=index, retry_attempt=retry_attempt)
                futures.append((future, index, num_parts, sentence))
            else:
                new_sentences[index] = [sentence]
```

**关键参数**:

- `max_split_length`: 粗切最大长度（默认 20 词）
- `max_workers`: 并发线程数（默认 4）

**输入**: `output/log/_3_1_nlp_processed.txt`
**输出**: `output/log/_3_2_meaning_processed.txt`

---

### 四、翻译阶段 (`_4_2_translate.py`)

#### 核心流程

1. **分块**: 按字符数和行数切分（600 字符，最多 10 行）
2. **术语加载**: 读取 AI 生成的术语表
3. **并发翻译**: 使用线程池并发翻译各块
4. **结果匹配**: 使用相似度匹配确保翻译顺序正确
5. **长度修剪**: 对过长翻译进行自动修剪

#### 翻译策略

使用 **三步翻译法**（在 `translate_lines.py` 中实现）：

1. **Faithfulness**: 忠实翻译
2. **Expressiveness**: 表达性优化
3. **Adaptation**: 本地化适配

#### 上下文感知

```python
def get_previous_content(chunks, chunk_index):
    return None if chunk_index == 0 else chunks[chunk_index - 1].split('\n')[-3:]  # 前 3 行

def get_after_content(chunks, chunk_index):
    return None if chunk_index == len(chunks) - 1 else chunks[chunk_index + 1].split('\n')[:2]  # 后 2 行
```

#### 结果验证

使用**序列匹配器**确保翻译块与原文块对应：

```python
def similar(a, b):
    return SequenceMatcher(None, a, b).ratio()

# 计算相似度
chunk_text = ''.join(chunk_lines).lower()
matching_results = [(r, similar(''.join(r[1].split('\n')).lower(), chunk_text))
                   for r in results]
best_match = max(matching_results, key=lambda x: x[1])

if best_match[1] < 0.9:
    console.print(f"[yellow]Warning: No matching translation found[/yellow]")
    raise ValueError(f"Translation matching failed")
```

**输入**: `output/log/_3_2_meaning_processed.txt` + `output/log/terminology.json`
**输出**: `output/log/_4_2_translation.xlsx`

---

### 五、字幕长度切割与对齐阶段 (`_5_split_sub.py`)

翻译后的字幕可能超过显示长度限制，需要进一步切割并与源语言对齐。

#### 核心功能

1. **长度检测**: 检测超长字幕
2. **二次分割**: 使用 LLM 将长字幕分成 2 部分
3. **双语对齐**: 使用 LLM 对齐源语言和目标语言的分割
4. **迭代优化**: 最多 3 轮，直到所有字幕符合长度要求

#### 字符宽度计算

不同语言字符占用不同宽度：

```python
def calc_len(text: str) -> float:
    def char_weight(char):
        code = ord(char)
        if 0x4E00 <= code <= 0x9FFF or 0x3040 <= code <= 0x30FF:  # 中文和日文
            return 1.75
        elif 0xAC00 <= code <= 0xD7A3 or 0x1100 <= code <= 0x11FF:  # 韩文
            return 1.5
        elif 0x0E00 <= code <= 0x0E7F:  # 泰文
            return 1
        elif 0xFF01 <= code <= 0xFF5E:  # 全角符号
            return 1.75
        else:  # 其他字符（如英文和半角符号）
            return 1

    return sum(char_weight(char) for char in text)
```

#### 长度判断逻辑

```python
MAX_SUB_LENGTH = subtitle_set["max_length"]  # 默认 75
TARGET_SUB_MULTIPLIER = subtitle_set["target_multiplier"]  # 默认 1.2

for i, (src, tr) in enumerate(zip(src_lines, tr_lines)):
    src, tr = str(src), str(tr)
    if len(src) > MAX_SUB_LENGTH or calc_len(tr) * TARGET_SUB_MULTIPLIER > MAX_SUB_LENGTH:
        to_split.append(i)
```

#### LLM 对齐 Prompt (`get_align_prompt`)

```
## Role
You are a Netflix subtitle alignment expert fluent in both {src_lang} and {targ_lang}.

## Task
We have {src_lang} and {targ_lang} original subtitles, as well as a pre-processed split version of {src_lang} subtitles.
Your task is to create the best splitting scheme for the {targ_lang} subtitles based on this information.

1. Analyze the word order and structural correspondence between languages
2. Split the {targ_lang} subtitles according to the pre-processed {src_lang} split version
3. Never leave empty lines
4. Do not add comments or explanations

## INPUT
<subtitles>
{src_lang} Original: "{src_sub}"
{targ_lang} Original: "{tr_sub}"
Pre-processed {src_lang} Subtitles ([br] indicates split points): {src_part}
</subtitles>

## Output in only JSON format
{
    "analysis": "Brief analysis of word order and structure...",
    "align": [
        {
            "src_part_1": "...",
            "target_part_1": "Corresponding aligned subtitle part"
        },
        {
            "src_part_2": "...",
            "target_part_2": "..."
        }
    ]
}
```

#### 迭代切割

```python
for attempt in range(3):  # 最多 3 轮
    console.print(Panel(f"🔄 Split attempt {attempt + 1}", expand=False))
    split_src, split_trans, remerged = split_align_subs(src.copy(), trans)

    # 检查是否所有字幕都符合长度要求
    if all(len(src) <= MAX_SUB_LENGTH for src in split_src) and \
       all(calc_len(tr) * TARGET_SUB_MULTIPLIER <= MAX_SUB_LENGTH for tr in split_trans):
        break

    # 更新源数据继续下一轮分割
    src, trans = split_src, split_trans
```

**关键参数**:

- `subtitle.max_length`: 字幕最大长度（默认 75）
- `subtitle.target_multiplier`: 目标语言倍数（默认 1.2）

**输入**: `output/log/_4_2_translation.xlsx`
**输出**:

- `output/log/_5_split_sub.xlsx` (用于显示的切分字幕)
- `output/log/_5_remerged.xlsx` (用于配音的重合并字幕)

---

### 六、时间戳对齐与 SRT 生成阶段 (`_6_gen_sub.py`)

这是字幕生成的最后阶段，将分割好的字幕与原始音频时间戳对齐，生成标准 SRT 文件。

#### 核心功能

1. **字符串匹配**: 将句子与词级时间戳匹配
2. **时间戳分配**: 为每个字幕分配开始和结束时间
3. **去除间隙**: 消除字幕之间的小间隙（< 1 秒）
4. **SRT 生成**: 生成多种格式的 SRT 文件
5. **标点优化**: 替换中文标点为空格

#### 时间戳匹配算法

核心算法是将**字幕句子**与**词级时间戳**精确匹配：

```python
def get_sentence_timestamps(df_words, df_sentences):
    time_stamp_list = []

    # 构建完整字符串和位置映射
    full_words_str = ''
    position_to_word_idx = {}

    for idx, word in enumerate(df_words['text']):
        clean_word = remove_punctuation(word.lower())
        start_pos = len(full_words_str)
        full_words_str += clean_word
        for pos in range(start_pos, len(full_words_str)):
            position_to_word_idx[pos] = idx  # 每个字符位置映射到词索引

    current_pos = 0
    for idx, sentence in df_sentences['Source'].items():
        clean_sentence = remove_punctuation(sentence.lower()).replace(" ", "")
        sentence_len = len(clean_sentence)

        match_found = False
        while current_pos <= len(full_words_str) - sentence_len:
            if full_words_str[current_pos:current_pos+sentence_len] == clean_sentence:
                start_word_idx = position_to_word_idx[current_pos]
                end_word_idx = position_to_word_idx[current_pos + sentence_len - 1]

                time_stamp_list.append((
                    float(df_words['start'][start_word_idx]),
                    float(df_words['end'][end_word_idx])
                ))

                current_pos += sentence_len
                match_found = True
                break
            current_pos += 1

        if not match_found:
            print(f"\n⚠️ Warning: No exact match found for sentence: {sentence}")
            raise ValueError("❎ No match found for sentence.")

    return time_stamp_list
```

**匹配策略**:

1. 去除所有标点符号
2. 转换为小写
3. 去除空格（对于非空格分隔语言）
4. 逐字符滑动窗口匹配

#### 去除间隙

消除字幕之间的小间隙，提升观看体验：

```python
for i in range(len(df_trans_time)-1):
    delta_time = df_trans_time.loc[i+1, 'timestamp'][0] - df_trans_time.loc[i, 'timestamp'][1]
    if 0 < delta_time < 1:  # 如果间隙小于 1 秒
        # 将当前字幕的结束时间延长到下一个字幕的开始时间
        df_trans_time.at[i, 'timestamp'] = (df_trans_time.loc[i, 'timestamp'][0], df_trans_time.loc[i+1, 'timestamp'][0])
```

#### SRT 时间格式转换

```python
def convert_to_srt_format(start_time, end_time):
    def seconds_to_hmsm(seconds):
        hours = int(seconds // 3600)
        minutes = int((seconds % 3600) // 60)
        seconds = seconds % 60
        milliseconds = int(seconds * 1000) % 1000
        return f"{hours:02d}:{minutes:02d}:{int(seconds):02d},{milliseconds:03d}"

    start_srt = seconds_to_hmsm(start_time)
    end_srt = seconds_to_hmsm(end_time)
    return f"{start_srt} --> {end_srt}"
```

#### 生成多种 SRT 格式

```python
SUBTITLE_OUTPUT_CONFIGS = [
    ('src.srt', ['Source']),                      # 仅源语言
    ('trans.srt', ['Translation']),               # 仅目标语言
    ('src_trans.srt', ['Source', 'Translation']), # 双语（源+译）
    ('trans_src.srt', ['Translation', 'Source'])  # 双语（译+源）
]

AUDIO_SUBTITLE_OUTPUT_CONFIGS = [
    ('src_subs_for_audio.srt', ['Source']),
    ('trans_subs_for_audio.srt', ['Translation'])
]
```

#### SRT 文件格式

```
1
00:00:00,000 --> 00:00:02,500
This is the first subtitle
这是第一条字幕

2
00:00:02,500 --> 00:00:05,000
This is the second subtitle
这是第二条字幕
```

**输入**:

- `output/log/_2_cleaned_chunks.xlsx` (词级时间戳)
- `output/log/_5_split_sub.xlsx` (切分后的字幕)
- `output/log/_5_remerged.xlsx` (配音用字幕)

**输出**:

- `output/src.srt`
- `output/trans.srt`
- `output/src_trans.srt`
- `output/trans_src.srt`
- `output/audio/src_subs_for_audio.srt`
- `output/audio/trans_subs_for_audio.srt`

---

## 📊 关键配置参数总览

### NLP 分割参数

```yaml
nlp:
  max_sentence_length: 60 # 句子最大长度
  min_split_length: 30 # 最小分割段长度
  comma_split_threshold: 3 # 逗号分割最小词数
```

### 字幕参数

```yaml
subtitle:
  max_length: 75 # 字幕最大长度（字符）
  target_multiplier: 1.2 # 翻译字幕长度倍数

max_split_length: 20 # LLM 粗切最大长度（词数）
```

### 翻译参数

```yaml
summary_length: 8000 # 摘要长度
max_workers: 4 # 并发线程数
reflect_translate: true # 是否反思翻译
pause_before_translate: false # 翻译前暂停编辑术语表
```

### 配音参数

```yaml
min_subtitle_duration: 2.5 # 最小字幕时长（强制延长）
min_trim_duration: 3.5 # 最小修剪时长
tolerance: 1.5 # 允许延长到下一字幕的时间

speed_factor:
  min: 1
  accept: 1.2 # 最大可接受语速
  max: 1.4
```

---

## 🔄 完整处理流水线示意图

```
┌─────────────────────────────────────────────────────────────────┐
│                        视频/音频输入                              │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 1: ASR 转录 (_2_asr.py)                                    │
│  • 视频转音频 (ffmpeg)                                           │
│  • Demucs 人声分离（可选）                                       │
│  • WhisperX 词级转录                                             │
│  输出: cleaned_chunks.xlsx (词级时间戳数据)                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 2: NLP 分割 (_3_1_split_nlp.py)                            │
│  ├─ 步骤 1: split_by_mark        (标点符号分割)                 │
│  ├─ 步骤 2: split_by_comma       (智能逗号分割)                 │
│  ├─ 步骤 3: split_by_connector   (连接词分割)                   │
│  └─ 步骤 4: split_long_by_root   (句法根动态规划分割)           │
│  输出: _3_1_nlp_processed.txt                                   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 3: LLM 语义分割 (_3_2_split_meaning.py)                    │
│  • 使用 GPT 对超长句子进行语义感知分割                           │
│  • 并发处理 + 3 轮迭代                                           │
│  • 序列匹配器定位分割点                                          │
│  输出: _3_2_meaning_processed.txt                               │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 4: 摘要与翻译 (_4_1, _4_2)                                 │
│  • 提取术语和主题                                                │
│  • 三步翻译法 (Faithfulness → Expressiveness → Adaptation)      │
│  • 上下文感知 + 并发翻译                                         │
│  输出: _4_2_translation.xlsx                                    │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 5: 字幕长度切割与对齐 (_5_split_sub.py)                    │
│  • 检测超长字幕（基于字符宽度计算）                              │
│  • LLM 二次分割                                                  │
│  • 双语对齐（使用 LLM）                                          │
│  • 迭代优化（最多 3 轮）                                         │
│  输出: _5_split_sub.xlsx, _5_remerged.xlsx                      │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 6: 时间戳对齐与 SRT 生成 (_6_gen_sub.py)                   │
│  • 字符串精确匹配算法                                            │
│  • 去除间隙优化                                                  │
│  • 生成多种 SRT 格式                                             │
│  输出: src.srt, trans.srt, src_trans.srt, trans_src.srt         │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 7: 字幕烧录 (_7_sub_into_vid.py)                           │
│  • 使用 ffmpeg 烧录字幕到视频                                    │
│  输出: output_sub.mp4                                           │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│  阶段 8-12: 配音流程（可选）                                     │
│  • 生成音频任务和块                                              │
│  • 提取参考音频                                                  │
│  • TTS 配音                                                      │
│  • 语速调整和合并                                                │
│  • 合并到视频                                                    │
│  输出: output_dub.mp4                                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 💡 核心创新点

### 1. 多层次混合分割策略

VideoLingo 采用了**规则 + AI** 的混合分割策略：

- **层次 1（NLP 规则）**: 基于语言学规则的快速粗分割

  - 标点符号
  - 逗号（带语法验证）
  - 连接词（带依存分析）
  - 句法根（动态规划）

- **层次 2（LLM 语义）**: 基于语义的智能细分割

  - 使用 GPT 理解句子语义
  - 保持语义完整性
  - 生成多个方案并比较

- **层次 3（长度对齐）**: 基于显示长度的二次分割
  - 考虑字符宽度差异
  - 双语对齐
  - 迭代优化

### 2. 精确的时间戳匹配算法

通过**逐字符滑动窗口匹配**，实现了句子与词级时间戳的精确对齐，即使在有标点符号差异的情况下也能准确匹配。

### 3. 上下文感知的翻译

翻译时提供前后文信息，确保翻译的连贯性和一致性：

- 前 3 行
- 后 2 行
- 术语表
- 主题摘要

### 4. 智能长度控制

#### 字符宽度感知

不同语言字符占用不同显示宽度：

- 中文/日文: 1.75
- 韩文: 1.5
- 泰文: 1.0
- 全角符号: 1.75
- 英文/半角: 1.0

#### 多轮迭代优化

最多 3 轮切割，确保所有字幕都符合长度要求。

### 5. Netflix 标准单行字幕

强制要求单行字幕，符合 Netflix 字幕规范，提升观看体验。

---

## 🎯 质量保证机制

### 1. 验证机制

- **分割验证**: 检查 LLM 返回的 JSON 格式和 `[br]` 标记
- **匹配验证**: 使用序列匹配器确保相似度 > 0.9
- **长度验证**: 多轮检查直到所有字幕符合长度要求

### 2. 错误处理

- **异常捕获**: `@except_handler` 装饰器
- **重试机制**: 3 轮迭代处理
- **日志记录**: Rich 库美化输出

### 3. 容错设计

- **低相似度警告**: 相似度 < 0.9 时发出警告
- **空行处理**: 自动合并到前一行
- **标点符号处理**: 特殊处理中文、日文标点

---

## 🚀 性能优化

### 1. 并发处理

- **翻译**: `ThreadPoolExecutor` 并发翻译多个块
- **分割**: 并发分割多个长句
- **配置化**: `max_workers` 参数控制并发数

### 2. 渐进式处理

- 逐阶段保存中间结果
- 支持断点续传
- 避免重复计算

### 3. 智能缓存

- 词级时间戳复用
- 术语表缓存
- 模型加载优化

---

## 📈 支持的语言

### 输入语言识别

- 🇺🇸 英语（优秀）
- 🇷🇺 俄语（良好）
- 🇫🇷 法语（优秀）
- 🇩🇪 德语（优秀）
- 🇮🇹 意大利语（优秀）
- 🇪🇸 西班牙语（优秀）
- 🇯🇵 日语（一般）
- 🇨🇳 中文（良好，使用专用模型）

### 翻译和配音

- 翻译支持所有语言
- 配音语言取决于 TTS 方法

---

## 🔧 技术栈

### 核心依赖

- **Web 框架**: Streamlit
- **ASR**: WhisperX (large-v3/large-v3-turbo)
- **NLP**: spaCy (多语言模型)
- **LLM**: OpenAI-Like API (Claude, GPT-4, DeepSeek, Gemini)
- **TTS**: Azure TTS, OpenAI TTS, Fish TTS, GPT-SoVITS, Edge TTS
- **音频处理**:
  - ffmpeg (转换、合并)
  - Demucs (人声分离)
- **其他**:
  - pandas (数据处理)
  - rich (终端美化)
  - autocorrect_py (文本校正)

---

## 📝 总结

VideoLingo 的字幕生成和分割逻辑体现了以下特点：

### ✅ 优势

1. **多层次分割**: NLP + LLM 混合策略，兼顾速度和质量
2. **精确对齐**: 词级时间戳 + 字符串匹配算法
3. **语义感知**: LLM 理解句子语义，保持翻译连贯
4. **Netflix 标准**: 单行字幕 + 长度控制
5. **多语言支持**: 8+ 语言输入，全语言翻译
6. **可配置性强**: 丰富的参数控制
7. **容错性好**: 多轮迭代 + 验证机制

### ⚠️ 局限性

1. **WhisperX 限制**:

   - 背景噪音影响转录质量
   - 数字字符可能被截断
   - 多语言视频只保留主语言

2. **LLM 依赖**:

   - 需要强模型才能保证 JSON 格式
   - API 成本

3. **配音限制**:

   - 语速差异影响效果
   - 无法区分多角色

4. **处理时间**: 完整流程较长

### 🎯 核心竞争力

1. **单行字幕**: 区别于多行字幕的传统方案
2. **翻译质量**: 三步翻译法 + 术语表 + 上下文
3. **无缝配音**: 语速调整 + 时间对齐

---

## 🔗 相关文件索引

### 主流程文件

- `st.py` - Streamlit Web 入口
- `core/_2_asr.py` - ASR 转录
- `core/_3_1_split_nlp.py` - NLP 分割
- `core/_3_2_split_meaning.py` - LLM 语义分割
- `core/_4_2_translate.py` - 翻译
- `core/_5_split_sub.py` - 长度切割对齐
- `core/_6_gen_sub.py` - SRT 生成

### NLP 分割模块

- `core/spacy_utils/split_by_mark.py` - 标点分割
- `core/spacy_utils/split_by_comma.py` - 逗号分割
- `core/spacy_utils/split_by_connector.py` - 连接词分割
- `core/spacy_utils/split_long_by_root.py` - 句法根分割

### Prompt 定义

- `core/prompts.py` - 所有 LLM Prompt

### 配置文件

- `config.yaml` - 全局配置

---

## 📌 建议与改进方向

### 短期优化

1. 添加更多语言的连接词规则
2. 优化字符宽度计算算法
3. 支持自定义分割策略

### 中期优化

1. 支持多角色配音
2. 优化 WhisperX 对数字的处理
3. 添加字幕样式自定义

### 长期规划

1. 支持多语言视频（保留所有语言）
2. 实时字幕生成
3. 视频场景感知的字幕分割

---

**报告生成时间**: 2025-12-29  
**项目版本**: 3.0.0  
**分析者**: Antigravity AI
